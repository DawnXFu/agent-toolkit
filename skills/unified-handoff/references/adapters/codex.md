# Codex Adapter

Install under `.agents/skills/unified-handoff/`. Invoke explicitly with `$unified-handoff` when useful, or select the skill through the client interface.

The Markdown artifact remains agent-neutral. On resume, read it completely, run the bundled staleness command, inspect current repository evidence, and begin with the first immediate next step. Do not depend on a proprietary thread ID.
