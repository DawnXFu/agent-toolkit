# Claude Code Adapter

Install under `.claude/skills/unified-handoff/` for project or user scope. Invoke the skill when the user requests a handoff, context save, pause, resume, or migration. Claude may suggest a handoff after substantial work but must not create one automatically.

On resume, read `HANDOFF.md`, run staleness, verify branch and assumptions, then begin with Immediate Next Steps item 1.
