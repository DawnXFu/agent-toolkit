# OpenCode Adapter

Install under `.agents/skills/unified-handoff/`; OpenCode may also discover `.opencode/skills/` or Claude-compatible locations depending on configuration.

Load the skill, read the latest validated Markdown handoff, run staleness, and continue from Immediate Next Steps item 1. Native session-reading plugins may be used as supplementary evidence, but the validated Markdown file remains the portable source of truth.
